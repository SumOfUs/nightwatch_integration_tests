machine:
  node:
    version: 5.1.0

dependencies:
  post:
    - wget http://goo.gl/IHP6Qw -O selenium-server-standalone.jar
    - java -jar selenium-server-standalone.jar:
          background: true

test:
  pre:
  override:
    - nightwatch

